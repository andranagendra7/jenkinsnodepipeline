#!/usr/bin/env groovy


pipeline {

        agent {
                  node() {
                         }
               }
        stages {
                 stage('build') {
                                 steps {
                                         echo "check the maven version"
                                       }
                                 }
               }
